{
  "proxy": {
    "type": "string",
    "description": "代理地址",
    "hint": "HTTP/SOCKS 代理地址，留空则不使用代理。例如 http://127.0.0.1:7890 或 socks5://127.0.0.1:1080",
    "default": ""
  },
  "rsshub_base_url": {
    "type": "string",
    "description": "默认 RSSHub 域名",
    "hint": "用于 RSSHub 路由检索与订阅链接拼接的默认域名。支持在 LLM 工具调用时通过 base_url 临时覆盖",
    "default": "https://rsshub.app"
  },
  "default_interval": {
    "type": "int",
    "description": "默认监控间隔（分钟）",
    "hint": "订阅未设置 interval 时使用",
    "default": 10
  },
  "minimal_interval": {
    "type": "int",
    "description": "订阅最小监控间隔（分钟）",
    "hint": "限制用户通过 /sub_set 命令设置单个订阅监控间隔的最小值，防止过于频繁抓取对RSS源服务器造成压力。例如设置为5，则用户不能设置小于5分钟的间隔",
    "default": 1
  },
  "timeout": {
    "type": "int",
    "description": "请求超时（秒）",
    "hint": "获取 RSS 源时的 HTTP 请求超时时间",
    "default": 30
  },
  "hash_history_min": {
    "type": "int",
    "description": "去重历史最小保留数量",
    "hint": "每个 Feed 至少保留多少条去重哈希记录，值越大越能避免历史回流重复推送，但会增加存储与比对开销",
    "default": 200
  },
  "hash_history_multiplier": {
    "type": "int",
    "description": "去重历史增长倍数",
    "hint": "按当前抓取条目数动态扩展历史窗口，实际窗口≈entries * 该倍数（再受最小值与硬上限约束）",
    "default": 2
  },
  "hash_history_hard_limit": {
    "type": "int",
    "description": "去重历史硬上限",
    "hint": "每个 Feed 最多保留的去重哈希数量，用于限制数据库行体积与监控循环内存/CPU开销",
    "default": 5000
  },
  "tracking_query_params": {
    "type": "list",
    "description": "链接去重时忽略的查询参数",
    "hint": "用于清理常见追踪参数（如 utm_source/gclid/fbclid）。仅建议填写明确的追踪参数名，避免误删业务语义参数",
    "default": [
      "utm_source",
      "utm_medium",
      "utm_campaign",
      "utm_term",
      "utm_content",
      "utm_id",
      "gclid",
      "fbclid",
      "mc_cid",
      "mc_eid",
      "spm",
      "ref",
      "ref_src"
    ]
  },
  "download_image_before_send": {
    "type": "bool",
    "description": "先下载媒体后发送",
    "hint": "开启后会先下载媒体到本地再推送，可复用缓存，但是在docker环境下需要共享数据卷",
    "default": false
  },
  "history_entry_limit": {
    "type": "int",
    "description": "历史条目推送限制",
    "hint": "限制每次推送时最多发送多少条历史条目，按发布时间降序取最新N条。0=不限制，建议10-50。注意：此限制在监控器级别生效，会影响所有订阅",
    "default": 10
  },
  "ffmpeg": {
    "type": "object",
    "description": "FFmpeg 配置",
    "hint": "用于控制视频转码、无声视频GIF转换及 FFmpeg 自动准备行为",
    "default": {
      "video_transcode": false,
      "video_transcode_timeout": 120,
      "gif_transcode": false,
      "gif_transcode_timeout": 60
    },
    "items": {
      "video_transcode": {
        "type": "bool",
        "description": "视频发送前自动转码",
        "hint": "开启后，所有平台发送视频前会统一转码为兼容的 H264/AAC MP4 格式。默认false，适合QQ官方等需要特定编码的平台。注意：转码会消耗CPU且耗时",
        "default": false
      },
      "video_transcode_timeout": {
        "type": "int",
        "description": "视频转码超时（秒）",
        "hint": "视频转码的最大允许时间。转换耗时取决于视频长度和分辨率，建议120-300秒",
        "default": 120
      },
      "gif_transcode": {
        "type": "bool",
        "description": "无声视频自动转GIF",
        "hint": "开启后，检测到无音频流的视频时会自动转换为高质量GIF。适合Bilibili等平台的动图/无声短视频。注意：转换可能耗时较长",
        "default": false
      },
      "gif_transcode_timeout": {
        "type": "int",
        "description": "GIF转换超时（秒）",
        "hint": "视频转GIF的最大允许时间。转换耗时取决于视频长度和分辨率，建议60-120秒",
        "default": 60
      }
    }
  },
  "failed_queue_capacity": {
    "type": "int",
    "description": "失败队列容量",
    "hint": "当推送因平台连接失败时，消息会进入失败队列等待重试。每个订阅最多保留多少条失败消息。0=禁用失败队列",
    "default": 50
  },
  "failed_queue_max_retries": {
    "type": "int",
    "description": "失败队列最大重试次数",
    "hint": "失败消息的最大重试次数，超过此次数的消息将被视为已耗尽",
    "default": 3
  },

  "deduplicate_multi_bot": {
    "type": "bool",
    "description": "单会话多 BOT 去重",
    "hint": "开启后，当同一会话中有多个 BOT 订阅了相同的 RSS 源，只有最早订阅的 BOT 会推送消息，避免重复推送",
    "default": true
  },
  "bootstrap_skip_history": {
    "type": "bool",
    "description": "首轮跳过历史条目",
    "hint": "开启后，首次监控仅建立去重历史不推送旧条目；关闭后首次监控会按抓取结果推送历史条目",
    "default": true
  },
  "debug_payload": {
    "type": "bool",
    "description": "推送调试字段",
    "hint": "开启后，会在每条推送末尾附加调试信息，便于观察 guid/id/link/published/updated 等原始字段",
    "default": false
  },
  "platform_shared_data": {
    "type": "object",
    "description": "平台共享数据源配置",
    "hint": "配置特定平台是否开启共享数据源。开启后，该平台下不同 BOT 的订阅操作被视为共享数据",
    "default": {
      "aiocqhttp": false
    },
    "items": {
      "aiocqhttp": {
        "type": "bool",
        "description": "aiocqhttp 平台共享数据源",
        "hint": "开启后，aiocqhttp 平台下所有 BOT 的订阅数据共享，任意 BOT 掉线时其他 BOT 可继续推送",
        "default": false
      }
    }
  },
  "sender_strategies": {
    "type": "object",
    "description": "平台发送策略配置",
    "hint": "配置各平台使用的发送策略。关闭特定平台策略后将使用默认发送策略",
    "default": {
      "telegram": true,
      "aiocqhttp": true,
      "weixin_oc": true
    },
    "items": {
      "telegram": {
        "type": "bool",
        "description": "启用 Telegram 专用发送策略",
        "hint": "启用后将使用 Telegram 优化策略（媒体优先、大小限制处理）。关闭则使用默认策略",
        "default": true
      },
      "aiocqhttp": {
        "type": "bool",
        "description": "启用 OneBot 专用发送策略",
        "hint": "启用后将使用 OneBot 优化策略（合并转发节点）。关闭则使用默认策略",
        "default": true
      },
      "weixin_oc": {
        "type": "bool",
        "description": "启用微信个人号专用发送策略",
        "hint": "启用后将使用微信个人号优化策略（单组件顺序发送）。关闭则使用默认策略",
        "default": true
      }
    }
  },
  "webui": {
    "description": "WebUI 管理界面配置",
    "type": "object",
    "items": {
      "enabled": {
        "description": "启用 WebUI",
        "type": "bool",
        "default": false,
        "hint": "⚠️ 开启后会启动 Web 服务器，如果与其他插件端口冲突请修改或关闭"
      },
      "host": {
        "description": "监听地址",
        "type": "string",
        "default": "0.0.0.0",
        "hint": "0.0.0.0=允许外部访问，127.0.0.1=仅本地访问"
      },
      "port": {
        "description": "监听端口",
        "type": "int",
        "default": 9191
      },
      "auth_enabled": {
        "description": "启用登录验证",
        "type": "bool",
        "default": true,
        "hint": "✅ 开启：访问需登录（推荐）\n❌ 关闭：无需登录（仅建议内网使用）"
      },
      "password": {
        "description": "访问密码",
        "type": "string",
        "default": "",
        "hint": "仅在登录验证开启时生效。留空将自动生成 6 位随机密码"
      },
      "session_timeout": {
        "description": "会话超时时间/秒",
        "type": "int",
        "default": 3600,
        "hint": "登录会话有效时长，超时需重新登录"
      }
    }
  }
}
